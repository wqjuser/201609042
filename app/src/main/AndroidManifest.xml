<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="test.wqj.cn.a20160904">

    <application

        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SecondActivity" />
        <activity android:name=".SpfActivity" />
        <activity android:name=".FileActivity" />
        <activity android:name=".SQLiteActivity" />
        <activity android:name=".CustomizedActivity" />
        <activity
            android:name=".TestBufterActivity"
            android:label="@string/title_activity_test_bufter"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".DrawActivity"

            ></activity>
        <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    </application>

</manifest>